{
  "name": "l1",
  "version": "0.6.1",
  "description": "Delayed and repeated code execution for games",
  "main": "./dist/index.js",
  "files": [
    "/dist/*"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "lint": "eslint src",
    "release": "yarn build && np --no-tests",
    "build": "rm -rf dist && parcel build ./src/index.js --no-cache --no-source-maps"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rymdkraftverk/level1.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "6.7.0",
    "eslint-config-airbnb-base": "14.0.0",
    "eslint-plugin-import": "2.18.2",
    "np": "5.1.3",
    "parcel-bundler": "1.12.4"
  }
}
